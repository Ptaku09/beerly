import React from 'react';

const SectionTitle = ({ children }: { children: string }) => {
  return <p className="font-poppins font-bold text-2xl mb-2 pr-3">{children}</p>;
};

export default SectionTitle;
