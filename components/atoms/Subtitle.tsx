import React from 'react';

const Subtitle = ({ children }: { children: string }) => {
  return <p className="text-gray-600 font-josefin font-[600] text-2xl">{children}</p>;
};

export default Subtitle;
